// src/app/about/page.tsx

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About",
  description: "Learn more about IT Resolved",
};

export default function About() {
  return (
    <main>
      <h1>not active</h1>
    </main>
  );
}
